 version: 2
 jobs:
   build:
     working_directory: ~/
     docker:
       - image: circleci/python:3.6.4
     steps:
       - checkout
       - run:
         command: |
           sudo pip install pipenv
           pipenv install
       - run:
         command: |
           pipenv run pytest test_sample.py
